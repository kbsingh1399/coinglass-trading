# ENGINE_1 DASHBOARD & FOOTPRINT DATA FIX — Engine_1.py

## Observed Dashboard Issues from Live Desktop Terminal
In the live `Engine_1_Live_Console` Rich dashboard:
1. **FP_Delta & FP_POC Showing Blank `--`**: `fmt(v, fresh)` in `render_table()` treats `v == 0.0` as empty and displays `[dim]--[/dim]`. Footprint Delta (`fp_delta`) and Point of Control (`fp_poc`) values should be formatted cleanly (e.g. `+125.00` / `65042.50`) instead of being replaced by `--`.
2. **Column Width & Truncation (`...`)**: Columns `FP_Del..`, `FP_POC`, and `ARM` are truncated with `...` (e.g. `HOLD LONG (+0.00...`, `SHORT (0.78) [5/...`) due to strict `no_wrap=True` column constraints.
3. **Footprint Data Ingestion Alignment**: Ensure `fp_delta` (buy volume minus sell volume / Footprint delta) and `fp_poc` (volume POC price) are correctly calculated from live Binance klines / Coinglass DOM and mapped into `AssetSnapshot`.

## Optimization Instructions for Arena.ai

1. Update `render_table()` in `Engine_1.py`:
   - Refactor `fmt()` so numeric `0.0` values for `fp_delta` display as `0.00` with `[green]`/`[red]` coloring for positive/negative delta.
   - Adjust column widths and text wrapping for `FP_Delta`, `FP_POC`, and `ARM` columns so signal names (e.g. `HOLD LONG (+0.45%)`, `SHORT (0.78)`) render cleanly without truncation.
2. Update `AssetSnapshot` update pipeline in `Engine_1.py` to ensure `fp_delta` and `fp_poc` update on every kline tick.

INSTRUCTIONS FOR ARENA.AI:
1. Provide exact Python code changes for `Engine_1.py` using ```python fencing and `# TARGET: Engine_1.py` labels.
2. Ensure code matches existing syntax, imports, and parameters.
3. Print 'This is the Test' and 'Test2' and 'Arena.ai'.