# ENGINE_1 AUTONOMOUS IMPROVEMENT CYCLE — PHASE 1: CVD DIVERGENCE & LIQUIDATION SIGNAL REFINEMENT

## LIVE TELEMETRY & SYSTEM STATE SUMMARY
- **`Engine_1.py` Dashboard**: `FP_Delta`, `FP_POC`, and `ARM` formatting patches deployed cleanly. Point of Control (`fp_poc`) prices and signed Footprint Deltas (`fp_delta`) format without truncation.
- **Active Strategies (7/7)**: `S1_Liquidation`, `S2_CVD_Momentum`, `S3_Trend_Follow`, `S4_Mean_Reversion`, `S5_Vol_Expansion`, `S6_OI_Momentum`, `S7_CVD_Divergence`.
- **Live Account Status**: Binance Futures Testnet USDT Wallet: `$5,012.58` | Equity: `$5,012.58` | Active Symbols: 14.

---

## OPTIMIZATION REQUIREMENTS FOR ARENA.AI
We are expanding signal quality during range-bound and volatile market regimes:

1. **Refine CVD Divergence Filter (`S7_CVD_Divergence`)**:
   - Enhance divergence detection between Spot CVD and Futures CVD in `ensemble_strategy_predictor.py`.
   - When Spot CVD is strongly positive (`> +500k USD`) while Futures price creates lower lows, boost long confidence score by `+0.15`.
   - When Spot CVD is strongly negative (`< -500k USD`) while Futures price creates higher highs, boost short confidence score by `+0.15`.

2. **Dynamic Volatility Scaling on ATR Stop-Loss**:
   - Ensure `binance_broker.py` order sizing dynamically scales ATR trailing stop multipliers based on 1-hour historical volatility regimes (`HV_24h > 3.0%` -> 1.8x ATR SL, `HV_24h < 1.5%` -> 1.2x ATR SL).

---

## INSTRUCTIONS FOR ARENA.AI
1. Provide exact code changes using markdown blocks starting with `# TARGET: <filename>` (`Engine_1.py` or `ensemble_strategy_predictor.py` or `binance_broker.py`).
2. Ensure zero syntax errors and seamless backwards compatibility.
3. Print 'This is the Test' and 'Test2' and 'Arena.ai' at the end of your response to confirm synchronization.
