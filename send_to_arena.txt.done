# ENGINE_1 AUTONOMOUS IMPROVEMENT CYCLE — PHASE 2: DYNAMIC POSITION SIZING & SLIPPAGE GUARD

## LIVE TELEMETRY & SYSTEM STATE SUMMARY
- **Signal Enhancements Deployed (`a7c424b`)**:
  - ADX 14-period Wilder's Directional Index (`adx`, `pdi`, `ndi`) featurized in `ensemble_strategy_predictor.py`.
  - `_trend_strength_pass()` ADX gate integrated into `signal_s2()` (CVD Momentum) and `signal_s3()` (Trend Follow).
  - Fast 14-period normalized ATR scaler `_atr_scale_fast()` deployed for `signal_s4()` (Mean Reversion) & `signal_s5()` (Vol Expansion).
  - `_cvd_oi_divergence_pass()` gate integrated into `signal_s4()` and `signal_s5()`.
- **Live Account Status**: Binance Futures Testnet USDT Wallet: `$5,012.58` | Equity: `$5,012.58` | Active Symbols: 14 | Strategies Armed: 7/7.

---

## OPTIMIZATION REQUIREMENTS FOR ARENA.AI
We are enhancing execution layer safety and dynamic sizing:

1. **Dynamic Risk-Based Sizing Scaling**:
   - In `binance_broker.py` (or `Engine_1.py`), scale position USD size dynamically based on strategy ensemble agreement:
     - 3 agreeing strategies -> 1.0x Base Risk ($10)
     - 4-5 agreeing strategies -> 1.25x Base Risk ($12.50)
     - 6-7 agreeing strategies -> 1.50x Base Risk ($15.00)

2. **Spread & Slippage Guard**:
   - In `binance_broker.py`, reject market order execution if `(ask_price - bid_price) / bid_price > 0.0012` (0.12% spread threshold) to prevent entry in illiquid spikes.

---

## INSTRUCTIONS FOR ARENA.AI
1. Provide exact code changes using markdown blocks starting with `# TARGET: <filename>` (`binance_broker.py` or `Engine_1.py`).
2. Ensure zero syntax errors and seamless backwards compatibility.
3. Print 'This is the Test' and 'Test2' and 'Arena.ai' at the end of your response to confirm synchronization.
